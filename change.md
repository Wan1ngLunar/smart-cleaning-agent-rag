# 项目改造记录

本文档记录项目在原始版本基础上的增量改造，帮助后续开发者了解每项修改的背景、实现方式和验证结果。

## 记录规则

- 每次新增或修改代码时，同步更新本文档。
- 每项变更应说明：原问题、修改内容、修改意义、验证方式和相关提交。
- 只记录实际完成并验证过的结果；未完成的验证明确标记为“待验证”。
- 密钥、用户隐私数据和完整日志不得写入本文档。
- 新增或修改代码时，为会话状态、缓存、数据边界和失败处理等非显然逻辑补充解释性注释。
- 项目文本统一使用 UTF-8，中文直接写为可读字符串，不使用 Unicode 转义序列。
- 从2026-07-30起，Git提交说明统一使用中文；已有英文提交历史保留，不改写历史。

## 2026-07-27：建立 Git 安全基线

### 原问题

- 项目目录不是有效的 Git 仓库，缺少可追踪的版本历史。
- 本地包含 API 密钥、虚拟环境、日志、Python 缓存和 Chroma 运行数据，直接执行 `git add .` 有误提交敏感信息和运行产物的风险。
- Windows 与其他系统的默认换行符不同，可能产生无意义的整文件差异。

### 修改内容

1. 初始化 Git 仓库，并创建原始项目基线提交。
2. 扩充 `.gitignore`，忽略以下本地内容：
   - `.venv/`、IDE 配置和 Python 缓存；
   - `config/secret.yml`、`.env` 等密钥文件；
   - `logs/` 和 `*.log`；
   - 所有 `chroma_db/`、新的 `storage/` 和旧的 `md5.text`；
   - `local_backup/` 本地备份目录。
3. 新增 `.gitattributes`：
   - Python、YAML、TXT、CSV 和 Markdown 文件在仓库内统一使用 LF；
   - PDF 和 SQLite 文件按二进制文件处理。

### 修改意义

- 防止 API 密钥、日志及本地数据库被提交到代码仓库。
- 为后续改造建立可回退、可审查的版本基线。
- 减少 Windows CRLF 与跨平台 LF 差异造成的无效 Git 变更。

### 验证结果

- `git check-ignore` 确认 `config/secret.yml` 和 `rag/chroma_db/chroma.sqlite3` 已被忽略。
- 基线提交后 `git status` 显示工作区干净。

### 相关提交

- `410d5d8 chore: initialize project baseline`

## 2026-07-27：统一 Chroma 数据库与导入状态路径

### 原问题

`config/chroma.yml` 原先使用相对路径 `chroma_db`。Chroma 会相对于进程的当前工作目录解析该路径，因此项目中产生了三份数据库：

- `chroma_db/`：0 条 embedding；
- `agent/chroma_db/`：0 条 embedding；
- `rag/chroma_db/`：326 条 embedding。

与此同时，文档 MD5 导入状态保存在项目根目录的 `md5.text`。数据库和导入状态可能互相不匹配，导致程序连接空数据库后仍将知识文档误判为“已经导入”。

### 修改内容

1. 将三份旧 Chroma 数据库和旧 `md5.text` 移动到本地备份目录：
   - `local_backup/chroma_before_fix/root_chroma_db`
   - `local_backup/chroma_before_fix/agent_chroma_db`
   - `local_backup/chroma_before_fix/rag_chroma_db`
   - `local_backup/chroma_before_fix/md5.text`
2. 修改 `config/chroma.yml`：
   - `persist_directory` 从 `chroma_db` 改为 `storage/chroma`；
   - `md5_hex_store` 从 `md5.text` 改为 `storage/ingested_md5.txt`。
3. 修改 `rag/vector_store.py`：
   - 使用 `get_abs_path` 将数据库和 MD5 配置转换为基于项目根目录的绝对路径；
   - 初始化时自动创建所需目录；
   - 使用 `self.md5_hex_store` 统一完成 MD5 文件的检查和写入；
   - 保留原有 `RecursiveCharacterTextSplitter` 文本切分逻辑。
4. 重新导入 `data/` 中的 6 份知识文档，生成唯一的新数据库。

### 修改意义

- 无论程序从哪个工作目录启动，都使用 `D:\agent+RAG\storage\chroma`。
- Chroma 数据库和文档导入状态被放在同一个 `storage/` 运行目录下。
- 避免连接空数据库、重复生成数据库或因旧 MD5 状态跳过文档的问题。
- `storage/` 被 Git 忽略，运行数据不会进入代码仓库。

### 验证结果

- `rag/vector_store.py` 通过 Python 语法检查。
- `git diff --check` 未发现补丁格式或空白错误。
- `storage/chroma/chroma.sqlite3` 已成功生成。
- `storage/ingested_md5.txt` 包含 6 条非空 MD5 记录。
- 新 Chroma 数据库包含 326 条 embedding。

### 待补充验证

- 再次执行文档导入后，确认 6 个文件均被跳过且 embedding 数量仍为 326。

### 相关提交

- `6ed6d73 fix: use a single absolute Chroma storage path`

## 2026-07-28：增加基于 thread_id 的多轮对话记忆

### 原问题

Streamlit 使用 `session_state` 显示历史消息，但 `ReactAgent.execute_stream` 每次只向模型传入当前用户问题。界面虽然显示多轮对话，模型实际上无法读取此前内容。

### 修改内容

1. 在 `ReactAgent` 中创建 LangGraph `InMemorySaver`。
2. 创建 Agent 时通过 `checkpointer` 参数启用消息状态保存。
3. `execute_stream` 新增 `thread_id` 参数，并通过 `configurable.thread_id` 标识会话。
4. Streamlit 为每个页面会话生成 UUID。
5. 新增“新建对话”按钮，同时更换 thread ID 并清空页面消息。
6. 同步修改 `react_agent.py` 底部的本地调试调用。

### 修改意义

- 同一个 thread ID 下，模型可以读取此前的用户消息和回答。
- 不同会话之间的历史互相隔离。
- 用户可以主动创建一段没有旧上下文的新对话。
- 当前使用内存存储，适合单机演示；应用重启后历史会清空。

### 验证结果

- `agent/react_agent.py` 和 `app.py` 通过 Python 语法检查。
- `ReactAgent` 初始化成功，输出 `agent_init_ok`，确认当前 LangChain 版本接受 `checkpointer` 参数。
- 在同一会话中先提供“房间面积 68 平方米、家里有两只猫”，下一轮能够准确复述这两项信息。
- 点击“新建对话”后再次询问上述信息，Agent 不再记得旧会话内容，确认新 thread ID 与旧历史隔离。

### 相关提交

- 与本条记录同一提交：`feat: add thread-scoped conversation memory`

## 2026-07-28：使演示工具数据确定且可追溯

### 原问题

用户ID、城市和月份通过随机函数返回，同一会话可能出现身份和报告月份变化。天气工具返回固定数据，却被描述为实时天气。CSV 使用字符串分割，无法正确处理带逗号的字段；查询工具类型标注为字符串，实际返回字典。

### 修改内容

1. 在 `config/agent.yml` 中集中配置演示用户、城市和天气数据。
2. 删除随机用户、随机城市和随机月份逻辑。
3. 使用系统日期返回真实当前月份。
4. 天气返回值和工具描述明确标注为“演示数据、非实时查询”。
5. 使用 `csv.DictReader` 按表头读取本地演示记录。
6. 增加必要列、空记录和重复用户月份校验。
7. 先在局部变量中完成数据加载，成功后再更新全局缓存。
8. 使用JSON字符串作为使用记录的统一返回格式。
9. 修改主Prompt和报告Prompt，明确演示数据边界和可用范围。
10. 将报告标题统一为“智扫通”。

### 修改意义

- 相同输入能够得到稳定、可复现的工具结果。
- Demo 数据不会再被误导性地描述为真实用户或实时天气。
- CSV 中包含逗号或引号时仍能正确解析。
- 工具类型声明、描述和实际返回值保持一致。
- 查询不到记录时进入明确的空结果分支，不再随机选择其他数据代替。

### 验证结果

- 确定性工具测试输出 `tool_context_ok`。
- 当前演示用户固定返回 `1001`。
- 当前月份正确返回 `2026-07`。
- CSV 成功加载10个用户、120条月度记录。
- 使用记录工具返回类型为字符串，测试输出 `return_type: str`。
- 查询不存在的月份时返回空字符串并记录WARNING。
- Prompt 边界扫描未发现将演示工具描述为真实能力的肯定性语句。
- 天气场景明确标注数据为演示数据或非实时天气。
- 用户1001在2025-06的报告生成成功，标题为“智扫通扫地机器人使用情况报告与保养建议”。
- 查询当前月份2026-07时，Agent 明确说明没有记录，未编造指标或改查随机月份。

### 相关提交

- 与本条记录同一提交：`fix: make demo tools deterministic and explicit`

## 2026-07-28：迁移环境变量并固定项目依赖

### 原问题

项目通过 `config/secret.yml` 加载 DashScope API Key，普通配置模块与敏感信息耦合。克隆仓库后缺少该本地文件会导致项目启动失败，同时项目没有依赖清单，无法在新环境中复现当前已验证的运行版本。

### 修改内容

1. 新增 `requirements.txt`，固定当前运行环境中的11个直接依赖及版本。
2. 新增 `.env.example`，公开声明项目需要 `DASHSCOPE_API_KEY`，但只保留安全占位符。
3. 创建被 Git 忽略的本地 `.env`，用于保存真实 DashScope API Key。
4. 从 `utils/config_handler.py` 中删除 `load_secret_config` 和 `secret_conf`。
5. 将普通 YAML 配置加载方式从 `yaml.load(..., FullLoader)` 改为 `yaml.safe_load`。
6. 在 `model/factory.py` 中使用 `python-dotenv` 从项目根目录加载 `.env`。
7. 系统环境变量优先于本地 `.env`，便于后续容器和服务器部署。
8. 新增 `get_required_env`，缺少必要变量时返回包含修复方法的明确错误。
9. 聊天模型和 Embedding 模型统一从 `DASHSCOPE_API_KEY` 环境变量获得凭据。
10. 在迁移验证通过后删除旧 `config/secret.yml` 及其临时备份。

### 修改意义

- 真实密钥不再保存在项目 YAML 配置中，也不会进入 Git。
- 新开发者可以根据 `.env.example` 创建自己的本地配置。
- 缺少环境变量时能够快速失败，避免进入模型 SDK 后才出现难以定位的错误。
- 精确依赖版本使项目更容易在其他电脑复现。
- `yaml.safe_load` 限制 YAML 只能构造安全的基础数据类型。

### 验证结果

- `pip check` 输出 `No broken requirements found.`。
- `.env` 命中 `.gitignore` 规则，不会出现在 Git 状态中。
- `.env.example` 仅包含 `DASHSCOPE_API_KEY` 安全占位符。
- `requirements.txt` 包含11个预期依赖，名称和版本与当前环境一致。
- 项目源码中不再存在 `secret_conf` 或 `load_secret_config` 引用。
- `model.factory` 通过 `.env` 成功创建 `ChatTongyi` 和 `DashScopeEmbeddings`。
- 移走旧 `secret.yml` 后，Embedding API 调用成功并返回非空向量。
- 缺失测试环境变量时成功触发清晰的 `RuntimeError`，输出 `missing_env_fail_fast_ok`。
- 旧 `config/secret.yml` 和临时备份均已删除，本地 `.env` 保留且被 Git 忽略。

### 相关提交

- 与本条记录同一提交：`chore: add reproducible environment configuration`

## 2026-07-29：建立自动化测试与代码质量基线

### 原问题

项目没有自动化测试和覆盖率统计，路径计算、演示工具、CSV 加载及文件处理只能依赖手工验证。`listdir_with_allowed_type` 在目录不存在时错误返回允许的文件后缀元组，调用方可能继续把 `"txt"` 和 `"pdf"` 当作文件路径处理。

### 修改内容

1. 新增 `requirements-dev.txt`，在运行依赖基础上固定 pytest、pytest-cov 和 Ruff 版本。
2. 新增 `pyproject.toml`，统一配置测试发现、覆盖率统计和 Ruff 检查规则。
3. 新增 `tests/conftest.py`，使用测试专用占位 Key，避免测试依赖真实 DashScope 凭据。
4. 新增配置与路径测试，验证项目根目录、统一 Chroma 路径和确定性演示配置。
5. 新增工具测试，验证固定用户上下文、真实当前月份、天气免责声明、CSV 幂等加载、JSON字符串返回和缺失记录分支。
6. 新增文件工具测试，验证 MD5、文件类型过滤和目录不存在的边界行为。
7. 修复 `listdir_with_allowed_type`：目录不存在时返回空元组，不再返回文件后缀。

### 修改意义

- 核心工具和配置变更可以通过自动化测试回归验证。
- 测试不调用真实聊天模型或 Embedding API，也不要求开发者提供真实测试凭据。
- 覆盖率报告为后续补测提供可量化基线。
- 文件扫描失败时返回与函数语义一致的空集合，避免产生虚假路径和级联错误。

### 验证结果

- 首轮测试为9项通过、1项失败，失败项准确定位目录不存在时的错误返回值。
- 修复后单项回归测试输出 `1 passed`。
- 完整测试输出 `10 passed, 1 warning`。
- `agent`、`model`、`rag` 和 `utils` 共356条可执行语句，136条未覆盖，总覆盖率为62%。
- 当前仍存在1条 `langchain-community` 停止维护的弃用警告，已作为依赖迁移技术债保留，未通过过滤规则隐藏。
- Ruff 首轮发现13个可安全修复问题，包括10个导入顺序问题和3个无占位符 f-string。
- Ruff 自动修复13项后再次检查输出 `All checks passed!`。
- Ruff 修复后完整测试仍为10项通过，覆盖率保持62%。
- README 编写待完成。

### 相关提交

- 与本条记录同一提交：`test: add automated quality baseline`

## 2026-07-29：为核心改造补充解释性注释

### 原问题

前序改造已经实现统一向量库、多轮记忆、确定性演示工具、环境变量和自动化测试，但部分新增代码只体现“做了什么”，没有说明“为什么这样做”。会话隔离、缓存发布、CSV 编码、环境变量优先级等设计容易被后来者误删或误改。

### 修改内容

1. 为 Agent 检查点、thread ID、运行时报告标记和 Streamlit 会话状态补充注释。
2. 为 Chroma 绝对路径、MD5 状态、分片重叠和“先入库后记录 MD5”的顺序补充说明。
3. 为演示数据边界、CSV 局部加载、表头校验、重复记录和 JSON 返回约定补充说明。
4. 为 `.env` 优先级、必要变量快速失败和模型客户端复用补充说明。
5. 为安全 YAML 加载、分块 MD5 和目录不存在时返回空元组补充说明。
6. 为 Demo 与 Chroma YAML 配置、测试配置和开发依赖增加行内说明。
7. 为自动化测试增加测试意图 docstring，并解释 Unicode 转义、缓存隔离和边界测试。
8. 清理过时的大段注释代码，并统一少量相关格式与类型标注。

### 修改意义

- 后续开发者能理解关键实现背后的约束，而不仅是阅读语法。
- 注释重点覆盖容易引发数据错位、会话串线、密钥覆盖和缓存污染的风险点。
- 测试失败时可以从测试 docstring 快速理解被保护的业务行为。
- 配置和依赖文件可以直接说明各配置组的用途。

### 验证状态

- Ruff 静态检查输出 `All checks passed!`。
- 完整自动化测试输出 `10 passed, 1 warning`。
- 覆盖率回归统计354条可执行语句、136条未覆盖，总覆盖率保持62%。
- 唯一警告仍为已记录的 `langchain-community` 弃用技术债。

### 相关提交

- 与本条记录同一提交：`docs: explain core implementation with comments`

## 2026-07-29：统一使用可读的UTF-8中文

### 原问题

部分测试为了绕过终端编码问题使用了Unicode转义序列，降低了代码可读性。终端编码问题不应该通过降低源码可读性解决。

### 修改内容

1. 新增 `.editorconfig`，统一文本文件编码为UTF-8、换行为LF。
2. 将测试中的Unicode转义替换为直接中文字符串。
3. 新增文本编码测试，验证项目文本能够按严格UTF-8解码。
4. 新增转义扫描测试，禁止源码和数据重新引入Unicode转义序列。

### 修改意义

- 中文断言和数据字段可以直接阅读。
- IDE编码设置在仓库层面统一。
- 编码损坏和Unicode转义会被自动化测试发现。
- 终端显示问题与源码存储规则相互分离。

### 验证结果

- 全仓库转义扫描无输出，源码和数据中未发现反斜杠加字母u的四位十六进制转义形式。
- Ruff静态检查通过：`All checks passed!`。
- 文本编码专项测试通过：`2 passed`。
- 完整自动化测试通过：`12 passed, 1 warning`。
- 当前警告是已知的`langchain-community`弃用提醒，不影响本次编码规范修改。

### 相关提交

- 与本条记录同一提交：`style: use readable UTF-8 Chinese literals`

## 2026-07-29：补齐面向运行与项目展示的README

### 原问题

项目缺少README，新的开发者无法快速了解系统能力、数据边界、架构关系、安装方式和验证命令。招聘者也难以从仓库首页快速识别项目的技术栈与工程化设计。

### 修改内容

1. 新增项目定位、核心能力和技术栈说明。
2. 使用Mermaid绘制用户界面、Agent、模型、工具、Chroma和本地数据之间的架构关系。
3. 说明多轮记忆、工具选择、RAG检索和报告Prompt切换的请求流程。
4. 增加项目目录结构，并解释`storage/`属于被Git忽略的运行产物。
5. 增加虚拟环境、依赖安装、密钥配置、知识库初始化和Streamlit启动步骤。
6. 为PowerShell命令增加用途注释，帮助不熟悉命令行的开发者理解操作意义。
7. 增加Ruff、pytest和覆盖率检查命令。
8. 增加可复现的演示问题，覆盖RAG、演示天气、多轮记忆、报告生成和会话隔离。
9. 总结路径稳定性、幂等导入、缓存发布、安全配置和自动化守卫等关键工程设计。
10. 明确内存会话、本地演示数据、单机Chroma和弃用依赖等当前限制及后续计划。

### 修改意义

- 仓库首页能够直接说明项目解决的问题、核心技术和工程质量。
- 新开发者可以根据README从零配置环境、初始化知识库并启动应用。
- 演示步骤与已经验证的项目能力对应，减少不可复现或夸大能力的描述。
- 已知限制被主动公开，便于后续确定持久化、评估、测试和部署工作的优先级。

### 验证结果

- Git差异检查无输出，未发现行尾空格或补丁格式错误。
- Ruff静态检查输出`All checks passed!`。
- 完整自动化测试输出`12 passed, 1 warning`，其中包含UTF-8编码和Unicode转义守卫。
- README中的覆盖率命令执行成功：共354条可执行语句、136条未覆盖，总覆盖率为62%。
- 唯一警告仍为已记录的`langchain-community`弃用提醒。

### 相关提交

- 与本条记录同一提交：`docs: add project README`

## 2026-07-29：增加持续集成质量门禁

### 原问题

Ruff、pytest和覆盖率只能由开发者在本机手动执行。遗漏检查后仍可能提交存在静态错误、测试回归或覆盖率下降的代码，仓库也缺少可以向招聘者展示的自动化质量验证流程。

### 修改内容

1. 新增`.github/workflows/ci.yml`。
2. 在代码推送到`master`或`main`、创建或更新Pull Request时自动运行工作流，并支持从GitHub页面手动触发。
3. 使用当前官方主版本`actions/checkout@v6`和`actions/setup-python@v6`。
4. 固定CI使用Python 3.13，并根据两个依赖文件缓存pip下载内容。
5. 将GitHub Token权限限制为只读仓库内容。
6. 使用测试专用DashScope占位值，不向工作流写入真实API Key。
7. 自动执行Ruff和完整pytest测试。
8. 设置60%覆盖率质量门禁，防止低于当前62%基线。
9. 在README中说明触发条件、执行步骤、密钥边界和远程仓库启用条件。

### 修改意义

- 每次推送和Pull Request都能获得一致、可审查的自动化验证结果。
- CI不依赖开发者本机环境或真实模型密钥。
- 覆盖率由展示指标变为可执行的最低质量要求。
- 同时兼容当前`master`分支和未来可能采用的`main`分支。

### 验证结果

- 工作流YAML按UTF-8读取并通过结构断言，输出`ci_yaml_ok`。
- Ruff静态检查输出`All checks passed!`。
- 本地执行与CI相同的pytest覆盖率门禁命令，输出`12 passed, 1 warning`。
- 实际覆盖率为61.58%，成功达到60%最低要求。
- 唯一警告仍为已记录的`langchain-community`弃用提醒。
- GitHub Actions首次在线运行完成，工作流和`Ruff and pytest`任务状态均为`Success`。

### 相关提交

- 与本条记录同一提交：`ci: add automated quality checks`

## 2026-07-30：发布GitHub仓库并展示CI状态

### 原问题

项目和提交历史只保存在本地，招聘者无法通过链接查看代码、README和工程化改造过程。README也没有展示持续集成的在线运行状态。

### 修改内容

1. 将本地主分支从`master`重命名为`main`。
2. 配置远程仓库`Wan1ngLunar/smart-cleaning-agent-rag`并完成首次推送。
3. 确认GitHub Actions自动触发，工作流状态为`Success`。
4. 在README标题下增加可点击的CI状态徽章。
5. 将README中“远程仓库尚未配置”的旧说明替换为首次在线验证结果。
6. 从后续计划中移除已经完成的CI徽章事项。

### 修改意义

- 简历可以直接链接到公开仓库，展示源码、提交历史、测试和设计文档。
- CI徽章能够在仓库首页直观显示最近一次自动化质量检查结果。
- 本地主分支与GitHub常用的`main`命名保持一致。
- 本地验证与云端Ubuntu环境验证共同证明项目具备跨环境复现能力。

### 验证结果

- 本地当前分支为`main`，`origin`的读取和推送地址均指向目标GitHub仓库。
- 首次在线CI工作流和`Ruff and pytest`任务均显示绿色`Success`。
- CI徽章使用目标仓库中`ci.yml`工作流的标准链接，本次文档提交推送后由GitHub渲染。
- Git差异检查无输出，未发现空白或补丁格式错误。
- Ruff静态检查输出`All checks passed!`。
- 完整自动化测试输出`12 passed, 1 warning`，其中包含UTF-8文本守卫。
- 唯一警告仍为已记录的`langchain-community`弃用提醒。

### 相关提交

- 与本条记录同一提交：`docs: add CI status badge`

## 2026-07-30：为RAG回答增加可追溯来源

### 原问题

RAG服务会把检索片段和完整元数据提交给模型，但最终只返回模型正文。用户无法判断回答依据了哪个知识文件，也看不到PDF页码。调试函数还会把完整Prompt、用户问题和知识内容输出到终端，不适合作为可公开演示的日志行为。检索为空时仍会调用模型，存在编造回答和无效API费用的风险。

### 修改内容

1. 删除RAG链中的完整Prompt打印函数。
2. 新增来源格式化逻辑，兼容Windows和Linux路径分隔符，只向用户展示文件名。
3. PDF优先使用`page_label`，缺失时将从0开始的`page`转换为从1开始的人类页码。
4. 为检索片段分配`[1]`形式的来源编号，相同文件和页码按首次出现顺序去重。
5. 修改RAG Prompt，要求模型在核心结论后使用输入中真实存在的编号。
6. 由服务层确定性追加“参考来源”列表，不依赖模型自行生成文件名。
7. 过滤空白知识片段；没有有效片段时直接返回明确说明，不调用模型。
8. 模型返回空文本时返回异常说明，同时保留已经检索到的来源。
9. 日志只记录片段数和来源数，不记录用户原始问题或完整知识内容。
10. 新增3项不调用真实Chroma和DashScope的单元测试，覆盖来源去重、PDF页码、空检索和空模型回答。
11. 更新README中的核心能力、处理流程、质量基线、演示问题、工程设计和后续计划。

### 修改意义

- 用户可以从回答中的编号追溯到具体知识文件和PDF页码。
- 来源列表由代码根据检索元数据生成，结果比完全依赖模型输出更稳定。
- 空检索不会进入生成阶段，降低幻觉风险和无效模型调用。
- 终端日志不再暴露完整用户问题和知识库上下文。
- RAG服务的关键边界可以在无数据库、无真实API Key的环境中自动回归。

### 验证结果

- 首轮专项测试发现新增日志遗漏`logger`导入，修复后3项RAG测试全部通过。
- Ruff静态检查输出`All checks passed!`。
- Git差异检查未发现空白或补丁格式错误，仅提示Git会将本地CRLF规范化为仓库LF。
- 完整测试输出`15 passed, 1 warning`。
- 核心模块共382条可执行语句、124条未覆盖，总覆盖率为68%。
- `rag_service.py`共63条可执行语句、2条未覆盖，覆盖率为97%。
- 60%持续集成覆盖率门禁通过，实际覆盖率为67.54%。
- 唯一警告仍为已记录的`langchain-community`弃用提醒。
- 在Streamlit中使用“小户型应该如何选择扫地机器人？请根据知识库回答并给出来源。”完成真实端到端验证。
- Agent正确调用`rag_summarize`，日志显示检索到3个有效片段并整理为3个来源。
- 页面正文包含行内来源编号，末尾显示真实文件名；PDF来源包含页码，相同文件和页码没有重复。
- 终端不再打印完整RAG Prompt或知识文档内容，只保留片段数和来源数等统计日志。
- 首次验证遇到`ConnectionResetError 10054`；确认是本机Clash Verge系统代理导致DashScope连接被重置，临时关闭系统代理后直连成功，未修改业务代码或添加代理环境变量。

### 相关提交

- 与本条记录同一提交：`feat: add traceable RAG sources`

## 2026-07-31至2026-08-01：建立RAG检索评估基线与混合拒答

### 原问题

项目只能通过人工提问观察RAG回答，缺少固定评估问题、预期知识来源和可量化的检索指标。因此无法客观说明检索效果，也没有数据依据来选择低相关性拒答阈值。

### 修改内容

1. 新增`evaluation/retrieval_cases.yml`，保存6条知识库正例和2条跨领域负例。
2. 每条正例明确记录预期来源文件，负例不设置预期来源。
3. 新增`evaluation/evaluate_retrieval.py`，以UTF-8读取并校验评估用例的结构、类型、唯一ID和预期来源。
4. 在`VectorStoreService`中新增带分数检索接口，支持指定`k`，未指定时复用Chroma配置。
5. 对无意义的非正整数`k`主动抛出异常，避免静默产生错误评估结果。
6. 评估脚本复用真实Chroma数据库，打印每条问题的Top-3排名、相关性分数、来源文件和内容预览。
7. 统一处理Windows和Linux路径分隔符，只使用来源文件名与预期结果比较。
8. 增加6条包含领域相似词的困难负例，覆盖实时价格、具体型号比较、行业股票、手持吸尘器、工业机械臂和路径规划代码。
9. 自动计算Hit@1、Hit@3、MRR、正例最低Top-1分数、负例最高Top-1分数和分数边界。
10. 在RAG配置中增加0.20的保守低分门槛，仅用于过滤明显无关片段，不将其当作最终可回答性阈值。
11. RAG服务改用带分数检索接口，在调用模型前过滤空白片段和低分片段，并校验门槛必须位于0到1之间。
12. 使用带预设分数的向量存储测试替身替代旧Retriever替身，新增“全部片段低分时不调用模型”的回归测试。
13. 在RAG Prompt中增加资料充分性判断，要求核对问题对象、范围和时效，不能把关键词相似当作资料足够。
14. 资料不足时模型只返回内部标记，服务层将其转换为统一拒答文本，并且不追加无关参考来源。
15. 新增高分但设备类别不匹配的拒答测试，以及Prompt和服务层内部标记一致性测试。

### 修改意义

- 固定用例让每次调整分片、Embedding模型、Top-K或阈值后都能进行同口径对比。
- 正例用于衡量正确知识能否进入靠前排名，负例用于观察无关问题的分数上界。
- 带分数接口为后续计算Hit@K、MRR和实现低相关性拒答提供基础。
- 片段预览可以帮助区分“来源文件正确”和“具体内容真正相关”。
- 困难负例证明相似度只衡量语义接近程度，不能单独代表资料可回答性。
- 低分粗过滤与System级资料充分性判断组合，在控制无效API调用的同时处理高分范围错配问题。
- 冒烟和全量两种真实评估模式兼顾日常开发速度与发布前完整验收。

### 验证结果

- 14条评估用例成功加载，其中6条正例、8条负例。
- 6条正例的预期来源全部排在Top-1，当前Hit@1和Hit@3均为100%，MRR为1.0。
- 正例Top-1最低分为0.4798，对应`dtof_navigation`。
- 简单负例Top-1最高分为0.0596，但困难负例最高分达到0.6617，对应`handheld_vacuum_repair`。
- 困难负例分数与正例发生重叠，负例最高分比正例最低分高0.1819，证明无法使用单一相似度阈值可靠区分可回答与不可回答问题。
- 极值中点0.5708不是可用阈值：它会放过0.6617的手持吸尘器负例，同时误拒0.4798的dToF正例。
- 两条负例出现相关性分数范围警告，是极不相关片段产生零附近或负分导致，不代表脚本执行失败。
- 低分预过滤相关代码通过Ruff检查，RAG专项测试为4项全部通过，完整测试为16项全部通过。
- 资料充分性判断相关代码通过Ruff检查，RAG专项测试增加至6项，完整测试增加至18项，均全部通过。
- 首次真实端到端可回答性评估为2/3通过：正常dToF问题和简单无关问题符合预期，但高分困难负例`handheld_vacuum_repair`仍被错误回答。
- 失败回答把扫地机器人的尘盒、滤网、滚刷和吸口维护建议迁移到了戴森手持吸尘器，说明仅在普通Prompt文本中声明资料充分性仍不足以稳定约束设备范围。
- 该负例Top-1分数为0.6617，不能通过提高相似度阈值修复，否则会同时误拒分数更低的正确问题。
- 将知识库范围规则提升为System消息，并把用户问题和检索资料单独放入Human消息，防止设备边界与输入数据混在同一层级。
- 明确知识库只覆盖扫地机器人和扫拖一体机器人，禁止把相似部件的处理建议迁移到手持吸尘器等其他设备。
- 新增消息角色测试，验证知识库边界位于System消息、用户问题和参考资料位于Human消息。
- 修复后Ruff检查通过，RAG专项测试增加至7项，完整测试增加至19项，均全部通过。
- 再次运行真实端到端可回答性评估，正常正例、简单负例和高分困难负例全部符合预期，结果由2/3提升为3/3通过。
- 端到端评估脚本增加默认冒烟模式和`--all`全量模式：默认仅运行3条关键路径以控制API用量，全量模式运行YAML中的全部用例。
- 默认冒烟评估再次达到3/3通过，全量真实模型评估达到14/14通过，其中6条正例均正常回答并附带来源，8条负例均返回统一拒答文本。
- 新增6项不调用真实Chroma和DashScope的评估单元测试，覆盖用例基线、Hit@K、MRR、分数重叠、无重叠阈值、冒烟/全量选择和回答验收规则。
- 最终Ruff全项目检查输出`All checks passed!`。
- 完整自动化测试输出`25 passed, 1 warning`，唯一警告仍为已记录的`langchain-community`弃用提醒。
- 核心模块覆盖率为67.25%，达到持续集成要求的60%门禁。
- 已暂存和未暂存差异均通过Git补丁格式检查，仅出现本地CRLF将由Git规范化为仓库LF的提示。

### 相关提交

- 本次改动使用中文提交说明：`功能：建立RAG评估与混合拒答机制`

## 2026-08-01至2026-08-02：将会话检查点迁移到SQLite

### 原问题

Agent使用`InMemorySaver`按`thread_id`保存多轮消息，Streamlit进程退出后所有模型会话状态都会丢失。页面刷新依赖同一进程内的`session_state`，无法证明会话能够跨应用重启恢复。

### 修改内容

1. 增加运行依赖`langgraph-checkpoint-sqlite==3.1.0`，并验证与当前LangGraph依赖兼容。
2. 在Agent配置中增加`storage/checkpoints/agent.sqlite3`检查点路径。
3. 新增SQLite Checkpointer工厂，将配置路径转换为项目绝对路径并自动创建父目录。
4. SQLite连接允许Streamlit跨线程使用，数据库占用时最多等待30秒，并启用WAL模式改善本地并发读写。
5. 显式使用LangGraph内置安全类型白名单进行Msgpack反序列化，不允许任意Python类型实例化。
6. 初始化失败时主动关闭连接，正常使用时将连接交给调用方管理生命周期。
7. 将`ReactAgent`的`InMemorySaver`替换为配置驱动的SQLite Checkpointer，并在Agent构建失败时释放连接。
8. 新增幂等`close()`方法，本地调试入口使用`finally`保证模型调用异常时仍关闭数据库。
9. 新增跨Agent实例恢复测试：首个实例写入消息并关闭后，第二个实例使用相同`thread_id`成功恢复，不同`thread_id`保持隔离。
10. 新增重复关闭测试，验证底层SQLite文件句柄在首次关闭后确实失效。
11. 为`ReactAgent`新增页面历史读取接口，只返回用户消息和无工具调用的最终AI文本，过滤工具调用、工具结果、空白和非文本内容。
12. 新增会话ID工具，使用UUID创建新会话；URL参数缺失、非法或异常过长时自动生成隔离的新ID。
13. 新增历史消息过滤和会话ID校验测试，确保恢复页面时不暴露内部工具状态，也不直接信任URL输入。
14. 使用Streamlit会话级资源缓存复用每个浏览器会话的Agent，并在资源释放时调用幂等`close()`。
15. 将合法`thread_id`写入URL查询参数，使浏览器在应用重启后仍能定位原会话。
16. 首次进入、切换URL或页面状态丢失时，从SQLite恢复模型状态并重建可展示的聊天历史。
17. “新建对话”同时更新URL、LangGraph会话键和页面消息，避免页面清空但模型仍读取旧历史。
18. 增加空输出保护，避免Agent未产生可展示文本时访问空列表导致页面异常。

### 修改意义

- 会话状态不再依赖Python进程内存，应用停止和重新启动后仍可恢复。
- SQLite适合当前单机演示，不需要额外部署数据库服务，同时保留未来迁移PostgreSQL Checkpointer的接口边界。
- URL中的UUID为用户提供可复现的会话定位方式，旧URL与新对话保持隔离。
- 页面历史从LangGraph真实状态恢复，避免页面展示记忆与模型实际记忆不一致。
- 显式连接关闭、WAL、超时等待和严格反序列化提升了资源管理、并发和本地数据安全性。
- 文档明确说明URL尚未绑定登录身份，避免将本地恢复能力误述为已有生产级权限系统。

### 验证结果

- `langgraph-checkpoint-sqlite`版本为3.1.0，`SqliteSaver`导入成功。
- 工厂代码通过Ruff检查。
- SQLite自动创建`checkpoints`和`writes`关键数据表。
- 数据库位于已被Git忽略的`storage/`目录，不会提交本地会话内容。
- SQLite工厂3项专项测试通过，覆盖表初始化、WAL、安全消息往返和白名单外类型降级。
- ReactAgent持久化2项专项测试通过，完整自动化测试增加至30项并全部通过。
- 历史提取与会话ID共3项新增测试通过，相关专项测试为5项，完整自动化测试增加至33项并全部通过。
- Streamlit页面自动生成带UUID的`thread_id`查询参数，同一完整URL在应用重启后成功恢复聊天记录和模型记忆。
- 点击“新建对话”后UUID发生变化、页面历史清空且无法读取旧会话；重新打开旧URL后旧历史再次恢复。
- `pip check`输出`No broken requirements found.`，未发现依赖冲突。
- 最终Ruff全项目检查输出`All checks passed!`。
- 完整自动化测试输出`33 passed, 1 warning`，唯一警告仍为已记录的`langchain-community`弃用提醒。
- 核心模块共491条可执行语句、141条未覆盖，总覆盖率为71.28%，达到持续集成要求的60%门禁。
- 已暂存和未暂存差异均通过Git补丁格式检查，没有实际错误。

### 相关提交

- 本次改动使用中文提交说明：`功能：实现SQLite持久化会话`

## 2026-08-02：增加Docker容器化运行支持

### 原问题

- 项目只能依赖本机Python虚拟环境启动，缺少可复现的容器运行方式。
- 本地`.env`、虚拟环境、Chroma数据库、SQLite会话、日志和备份不应进入镜像构建上下文。
- 容器删除或重建后需要保留知识索引、会话检查点和运行日志。
- 应用需要以普通用户运行，同时仍能写入`storage`和`logs`运行目录。

### 修改内容

1. 新增`.dockerignore`，排除Git内部数据、IDE配置、虚拟环境、Python缓存、测试产物、真实环境变量文件、本地存储、日志和备份。
2. 保留不包含真实密钥的`.env.example`，真实`.env`不进入镜像构建上下文。
3. 新增`Dockerfile`，使用官方`python:3.13-slim`基础镜像，与项目验证版本保持一致。
4. 先复制并安装固定版本的`requirements.txt`，再复制业务代码，使仅修改代码时可以复用依赖安装缓存。
5. 设置Python即时日志、禁止字节码和pip缓存等容器环境选项。
6. 创建无登录权限的`appuser`普通用户，应用不以`root`身份运行。
7. 提前创建`/app/storage`和`/app/logs`，只将必要运行目录的写权限交给`appuser`。
8. 暴露Streamlit的8501端口，并绑定`0.0.0.0`以允许宿主机浏览器访问。
9. 使用Streamlit内置健康接口增加Docker健康检查，该检查不调用DashScope模型。
10. 新增`compose.yml`，统一配置镜像构建、容器名称、轻量init进程、端口映射和重启策略。
11. Compose通过本地`.env`在运行时注入DashScope密钥，不在Dockerfile或镜像中保存真实值。
12. 将宿主机`storage/`和`logs/`分别挂载到容器运行目录，使向量库、SQLite会话和日志在容器重建后保留。
13. 在README补充Docker Compose构建、知识库初始化、启动、状态检查、日志查看、停止和代理安全说明。

### 修改意义

- 新开发者只需准备Docker Desktop和本地`.env`即可复现统一的Python与依赖环境。
- 构建上下文过滤和运行时密钥注入降低了将API Key写入镜像历史的风险。
- 非root运行、最小写权限和健康检查提升了容器的安全性与可运维性。
- Compose将较长的`docker run`参数保存为可审查配置，并提供一条命令启动的演示方式。
- 宿主机目录挂载使容器生命周期与业务数据生命周期分离，重启或重建不会丢失本地知识索引和会话。
- 网络代理不固化到仓库，避免把个人代理地址、端口或凭据传播给其他开发者。

### 实施过程中的修正

- 首次构建发现Dockerfile的JSON形式`CMD`不能像Python列表一样直接跨行，将其改为单行JSON数组后通过解析。
- 首次运行发现普通用户无法创建`/app/logs`，在镜像构建阶段同时创建`storage`和`logs`并授予定向写权限后修复。
- Docker Hub基础镜像下载曾因本机代理路径超时；通过Docker Desktop代理设置完成镜像拉取，没有把代理配置写入仓库。
- 容器访问DashScope曾出现`SSLEOFError`；对比Windows与容器的直连TLS后，最终使用Docker Desktop的`No proxy`模式直连，未关闭证书验证或在业务代码中加入不安全绕过。

### 验证结果

- Docker客户端、Docker Desktop服务端和Docker Compose版本检查正常，官方`hello-world`容器运行成功。
- `docker build --tag smart-cleaning-agent:local .`成功构建本地镜像。
- `docker compose config --quiet`无输出，Compose配置校验通过。
- `docker compose up --build --detach`成功启动服务，`docker compose ps`显示容器状态为`healthy`。
- 浏览器可通过`http://localhost:8501`访问应用，真实RAG问题能够正常生成回答并显示参考来源。
- 容器内`id`结果显示应用使用`appuser`而不是`root`运行。
- 镜像配置检查确认不包含`DASHSCOPE_API_KEY`，真实密钥仅存在于运行时容器环境。
- 重启Compose服务后，同一`thread_id`的聊天记录能够从挂载的SQLite检查点恢复。
- 宿主机`storage/`和`logs/`目录均存在，运行数据和日志挂载生效。
- Windows虚拟环境和容器分别直连DashScope根地址，均返回HTTP 404并完成TLS握手，证明最终容器网络路径可用。

### 相关提交

- 本次改动使用中文提交说明：`功能：增加Docker容器化运行支持`

## 2026-08-03：增加请求追踪与友好错误处理

### 原问题

- DashScope或其他Agent步骤异常时，Streamlit会直接展示完整Python调用栈、SDK路径和网络细节，用户体验与信息边界不符合公开演示要求。
- 原工具中间件会记录完整工具参数，模型调用前的debug日志会记录最后一条消息正文，可能把用户问题、用户编号、月份或检索内容写入持久化日志。
- Agent、模型和工具日志没有统一标识，无法判断多条日志是否属于同一次提问。
- 日志只记录调用开始或结果，缺少模型、工具和整次请求的耗时数据。
- 外部演示记录未命中时会把查询用户编号和月份写入警告日志，与工具参数最小化规则冲突。

### 修改内容

1. 每次`ReactAgent.execute_stream()`执行时生成12位十六进制`request_id`，只记录`thread_id`前8位作为非完整会话前缀。
2. 使用`perf_counter()`计算整次Agent请求耗时，记录请求开始、成功、失败、问题编号、会话前缀和毫秒耗时。
3. 新增`AgentExecutionError`，服务端通过`logger.exception()`保留真实异常类型、调用栈和因果链，页面异常只包含安全说明与问题编号。
4. Streamlit使用同一助手消息容器渲染正常回答或安全错误；失败时不立即重新运行页面，避免错误提示消失。
5. 使用LangChain`wrap_model_call`替换原模型调用前日志，记录同一问题编号、消息数量、成功或失败状态及模型调用耗时。
6. 工具中间件记录同一问题编号、工具名称、成功或失败状态及工具调用耗时，不再输出工具参数。
7. 删除最后一条模型消息正文的debug日志，不在请求生命周期日志中保存用户完整问题或Prompt内容。
8. 工具和模型失败后使用原始`raise`继续抛出，避免重置异常栈；Agent边界统一转换为页面可处理的安全异常。
9. 保留`fill_context_for_report`成功后切换报告Prompt的既有行为，并增加回归测试。
10. 将演示使用记录未命中日志改为通用说明，不再记录用户编号和月份。
11. 新增2项ReactAgent请求边界测试，覆盖成功日志、问题编号格式、查询脱敏、异常包装和因果链。
12. 新增4项模型与工具中间件测试，覆盖成功、失败、耗时、报告模式切换、消息正文和工具参数脱敏。
13. 扩充演示工具未命中测试，验证日志保留告警但不包含用户编号和月份。
14. 在README补充请求处理流程、问题编号排障方式、日志数据边界、测试基线、关键设计和当前限制。

### 修改意义

- 用户遇到模型或网络故障时只看到简洁提示，不会接触内部文件路径、SDK实现和调用栈。
- 页面问题编号可以与服务端Agent、模型和工具日志一一对应，缩短本地排障时间。
- 分层耗时能够区分延迟主要来自模型、知识检索、工具还是整体编排，为后续性能优化提供依据。
- 日志最小化降低了把用户输入、工具参数、会话访问标识和密钥写入日志文件的风险。
- 真实异常仍保存在开发者日志和异常因果链中，友好页面错误不会牺牲诊断能力。
- 全部新增测试使用内存假Agent、LangChain官方假模型和固定处理函数，不调用真实DashScope或业务工具。

### 验证结果

- 正常页面请求成功生成带来源的RAG回答；Agent、模型和工具日志使用同一12位问题编号，并记录各阶段毫秒耗时。
- 使用仅在测试终端生效的无效DashScope密钥完成失败验收，页面只显示友好说明和问题编号，没有显示`Traceback`、SDK路径或内部异常类型。
- 页面问题编号与模型失败和Agent失败日志中的`request_id`完全一致，服务端日志保留真实异常堆栈。
- 日志检查确认没有出现完整用户问题、工具查询参数、完整`thread_id`或API Key。
- Docker Compose使用最新代码成功重建镜像并启动，容器健康状态为`healthy`。
- 容器内真实RAG回答正常，容器日志中的`agent_request`、`model_call`和`tool_call`使用同一问题编号。
- `pip check`未发现依赖冲突，Ruff全项目检查输出`All checks passed!`。
- 完整自动化测试输出`39 passed, 1 warning`，唯一警告仍为已记录的`langchain-community`弃用提醒。
- 核心模块共527条可执行语句、122条未覆盖，总覆盖率为76.85%，达到持续集成要求的60%门禁。
- Compose配置校验无输出，已暂存和未暂存补丁格式检查没有实际错误，仅有本地CRLF与仓库LF规范化提示。

### 相关提交

- 本次改动使用中文提交说明：`功能：增加请求追踪与友好错误处理`

## 2026-08-04：迁移弃用的模型集成与文件加载器

### 原问题

- 项目使用`langchain-community`中的`ChatTongyi`、`DashScopeEmbeddings`、`PyPDFLoader`和`TextLoader`，运行测试时持续出现该包停止维护的弃用警告。
- 直接删除旧依赖可能改变向量维度或语义结果，使现有Chroma索引无法继续查询。
- `text-embedding-v4`单次最多接收10段文本，若沿用OpenAI客户端默认的较大批量，重新入库时可能超过DashScope接口限制。
- 文件加载器迁移后仍需保留RAG引用依赖的来源路径、PDF内部页索引和展示页码。

### 修改内容

1. 在虚拟环境中安装并在`requirements.txt`固定`langchain-openai==1.4.1`。
2. 使用同一段中文分别调用旧、新向量客户端，确认两者均返回1024维向量，余弦相似度为1.00000000且数值全部有效。
3. 使用强制工具调用验证`ChatOpenAI`能够通过DashScope兼容接口返回LangChain可识别的工具名称和参数。
4. 在`config/rag.yml`集中配置DashScope中国内地OpenAI兼容地址、1024维向量和10条批量上限。
5. 将模型工厂从`ChatTongyi`和`DashScopeEmbeddings`迁移为`ChatOpenAI`和`OpenAIEmbeddings`。
6. 为向量客户端显式设置1024维、10条批量大小，并关闭OpenAI专用的本地上下文切分；项目继续使用既有文本切分器控制知识片段。
7. 使用Python标准库`Path.read_text(encoding="utf-8")`替代`TextLoader`，将每个TXT文件转换为一个带`source`元数据的`Document`。
8. 使用`pypdf.PdfReader`替代`PyPDFLoader`，按页生成`Document`并保留`source`、从0开始的`page`及用于展示的`page_label`。
9. 增加TXT和PDF加载器单元测试，覆盖可读中文、来源路径、空白页正文、PDF分页和页码元数据。
10. 确认源码不再导入旧包后，从运行依赖和虚拟环境中删除`langchain-community`与旧`dashscope` SDK。
11. 重新构建Docker镜像，确认容器内同样不包含旧包，并使用新的聊天与向量客户端正常完成真实RAG回答。
12. 更新README中的技术栈、质量基线、关键设计、当前限制和后续计划。

### 修改意义

- 消除停止维护依赖带来的弃用警告和后续兼容风险，同时继续使用阿里云DashScope模型服务。
- 使用官方OpenAI兼容协议降低模型客户端与单一厂商SDK实现的耦合。
- 显式固定接口地址、向量维度和批量上限，使配置可审查，并避免请求误发、索引维度冲突和批量超限。
- 新旧向量结果完全一致，现有Chroma数据库无需重建，历史检索基线可以继续使用。
- 本地TXT/PDF加载逻辑更轻量、直接，并继续支持回答中的文件名和PDF页码引用。
- 本地虚拟环境、全新依赖安装和Docker镜像使用同一份直接依赖清单，减少环境差异。

### 验证结果

- `pip check`输出`No broken requirements found.`，卸载旧包后未出现依赖缺失或版本冲突。
- 新旧`text-embedding-v4`向量维度均为1024，余弦相似度为1.00000000，所有向量值均为有限数。
- 新聊天客户端返回1次`get_demo_city`工具调用，工具参数为无参数函数对应的空对象。
- 模型工厂成功创建`ChatOpenAI`和`OpenAIEmbeddings`，真实向量请求返回1024维结果。
- 新TXT加载器读取中文内容并保留来源，新PDF加载器按页返回文档并保留正确页码。
- Ruff全项目检查输出`All checks passed!`。
- 完整自动化测试输出`41 passed`，不再出现`langchain-community`弃用警告。
- 核心模块测试覆盖率为77.57%，达到持续集成要求的60%门禁。
- 真实检索评估保持Hit@1 100%、Hit@3 100%、MRR 1.0000。
- 真实模型可回答性冒烟评估为3/3通过，全量14条评估为14/14通过。
- Docker Compose重新构建后容器状态为`healthy`，容器内`pip check`通过，旧包不存在，新模型和文件加载器导入正常。
- 容器页面能够正常生成带参考来源的中文RAG回答，最近日志中没有旧依赖弃用警告。

### 相关提交

- 建议使用中文提交说明：`重构：迁移模型集成并移除弃用依赖`

## 2026-08-05：扩充60条RAG评测集并建立余弦检索基线

### 原问题

- 原评测集只有6条正例和8条负例，样本规模不足以支撑简历中的量化结论。
- 负例主要覆盖少量跨领域问题，对实时外部数据、相邻清洁设备、内部元器件维修、软件、法律和医疗边界覆盖不足。
- Chroma集合依赖默认L2距离；极不相关结果会被LangChain换算为小于0的相关性分数并产生警告，距离语义也没有写入项目配置。
- 原有来源级Hit@K只判断文件名，无法识别“命中了正确文件，但具体片段不包含答案”的情况。
- `选购指南.txt`将低电量自动回充条件错误写成剩余电量大于等于20%，与另一份知识资料冲突。

### 修改内容

1. 将`evaluation/retrieval_cases.yml`扩充到60条，其中36条正例覆盖6份知识文档，24条负例覆盖实时与外部数据、相邻设备、高风险拆机维修、程序开发、固件逆向、法律结论和医疗建议。
2. 保留稳定用例ID、问题类型和预期来源，并用自动化测试守卫总数、正负例数量、冒烟用例和关键边界用例，防止后续误删评测覆盖。
3. 将`选购指南.txt`中的低电量条件修正为“剩余电量小于等于20%时自动回充”，与设备正常行为和另一份知识资料保持一致。
4. 扩充RAG System规则：内部电芯、主板、激光雷达和220V电源等元器件级维修，以及医疗治疗和法律结论，在资料不具备专业依据时必须统一拒答。
5. 在`config/chroma.yml`显式配置`distance_metric: cosine`，使文本检索采用更符合语义向量场景的余弦距离。
6. 向量服务启动时校验距离度量只能为`cosine`、`l2`或`ip`，并通过`collection_configuration`将选择真正传给Chroma HNSW集合。
7. 增加配置测试和Chroma构造参数测试；测试使用临时目录和假Chroma对象，不读写真实`storage`数据库，也不调用Embedding接口。
8. 在删除当前L2集合前，将326个向量和6条MD5记录备份到被Git忽略的本地目录；只重建`storage/chroma`和导入状态，保留SQLite会话检查点。
9. 使用6份知识文件重建余弦索引，并验证集合距离度量为`cosine`、向量数量为326、MD5数量为6。
10. 保存L2与余弦两套60条检索输出，以及余弦距离下的60条真实端到端输出，便于后续算法实验复用同一基线。

### 修改意义

- 评测规模从14条提升到60条，可以分别观察正常知识召回和困难知识边界，不再依赖少量演示问题得出结论。
- 显式距离配置和索引重建消除了对Chroma默认值的依赖，使开发、测试和容器环境的检索语义一致。
- L2与余弦距离使用同一批归一化向量和同一套用例，能够形成可复现的对照实验，而不是用不同数据比较不可比指标。
- 正负例分数重叠证明不能依靠单一相似度阈值完成拒答，保留“低分粗过滤加模型资料充分性判断”的两层机制有真实数据依据。
- 端到端评测发现了来源级Hit@K无法暴露的片段漏召回，为P3混合检索、查询改写、重排序和片段级证据指标提供固定优化目标。
- 对唯一失败用例保留真实结果，不修改问题或放宽安全边界追求表面上的100%，使基线更可信。

### 验证结果

- 60条评测集包含36条正例和24条负例。
- 默认L2基线为Hit@1 80.56%、Hit@3 94.44%、MRR 0.8750；正例最低Top-1分数为0.4798，负例最高Top-1分数为0.6617，分数间隔为-0.1819。
- 显式余弦基线为Hit@1 80.56%、Hit@3 94.44%、MRR 0.8750；正例最低Top-1分数为0.6322，负例最高Top-1分数为0.7608，分数间隔为-0.1286。
- 两种距离的来源排名指标完全一致；余弦评测中的相关性分数警告数量为0。
- 真实模型冒烟评估为3/3通过；60条端到端评估为59/60通过，其中35/36条正例回答并附带来源，24/24条负例全部拒答。
- 唯一失败用例`low_battery_immediate_recharge`的正确20%回充证据未进入Top-10，模型因资料不足返回统一拒答，确认问题位于检索层而不是生成或阈值层。
- Ruff全项目检查输出`All checks passed!`。
- 完整自动化测试输出`43 passed`，没有旧模型集成的弃用警告。
- 核心模块共538条可执行语句、121条未覆盖，总覆盖率为77.51%，达到持续集成要求的60%门禁。
- Docker Compose配置校验无输出；已暂存和未暂存补丁格式检查没有实际错误，仅有本地CRLF与仓库LF规范化提示。

### 相关提交

- 建议使用中文提交说明：`评估：扩充60条RAG用例并建立余弦基线`

## 2026-08-06至2026-08-08：使用FastAPI解耦前后端服务

### 原问题

- Streamlit直接导入并创建`ReactAgent`，页面层同时负责UI、模型编排、SQLite连接和错误处理，职责耦合较重。
- 每个浏览器会话持有独立Agent和SQLite连接，增加了资源数量，也无法通过标准HTTP接口复用后端能力。
- 原单容器只运行Streamlit，无法独立扩缩、检查或替换前后端服务。
- 项目缺少可审查的请求/响应契约、API参数校验、HTTP客户端错误边界和后端接口测试。

### 修改内容

1. 固定`fastapi==0.141.1`、`uvicorn==0.52.1`和`httpx==0.28.1`运行依赖，并通过全新环境解析演练和`pip check`验证兼容性。
2. 新增`backend`包，使用Pydantic严格模型定义健康、聊天、历史和错误响应；拒绝额外字段、非法UUID4、空问题及超过4000字符的输入。
3. 使用FastAPI应用工厂和`lifespan`管理ReactAgent：服务启动时创建一次共享实例，关闭时在`finally`中释放SQLite连接。
4. 新增`GET /health`、`POST /api/v1/chat`和`GET /api/v1/sessions/{thread_id}/messages`，并通过依赖注入复用应用状态中的Agent。
5. 聊天接口收集LangGraph阶段输出并返回最后一条有效文本；Agent失败时返回502、安全说明和12位问题编号，空结果也不伪造回答。
6. 新增`frontend`包，使用复用连接池的HTTPX客户端调用聊天和历史接口，分别设置10秒连接超时和120秒总超时，并校验成功与错误响应结构。
7. 使用`API_BASE_URL`环境变量区分本机和容器地址；系统环境变量优先于`.env`，地址必须使用HTTP或HTTPS协议。
8. 重构`app.py`，删除对`ReactAgent`和`AgentExecutionError`的直接导入；页面只负责URL会话、HTTP调用、安全提示和本地打字效果。
9. 增加AST架构测试，阻止Streamlit入口重新绕过FastAPI直接依赖Agent。
10. 根据Starlette当前TestClient导入顺序，在开发依赖中固定`httpx2==2.7.0`，消除旧HTTPX兼容层弃用警告；生产前端继续使用`httpx`。
11. 将Dockerfile改为api/web共用的非root镜像，默认入口为FastAPI，并由Compose为两个服务覆盖独立命令。
12. 将Compose拆成`api`与`web`：前者独占模型密钥、Chroma、SQLite和日志挂载，后者只获得内部地址`http://api:8000`。
13. 为两个服务增加独立健康检查，`web`等待`api`健康后启动；宿主机分别开放8000和8501端口。
14. 扩大coverage配置，将`backend`和`frontend`纳入CI的60%覆盖率门禁，并增加API、客户端、配置、架构和容器安全测试。

### 修改意义

- UI、HTTP服务和Agent编排形成清晰边界，Streamlit可以替换为其他前端，FastAPI也可以独立供其他客户端调用。
- Pydantic输入输出校验、版本化路由和统一HTTP状态使接口行为可测试、可文档化，并为后续认证、限流和流式协议保留扩展点。
- 后端集中管理Agent与SQLite生命周期，前端只管理HTTP连接池，减少页面层资源职责和直接数据库访问。
- 双容器通过内部DNS通信；模型密钥和运行数据只交给api，降低前端容器被利用时的数据暴露范围。
- Swagger、双健康检查、服务依赖和独立日志使部署状态比单个Streamlit进程更容易诊断。
- 使用假Agent和MockTransport覆盖正常、空响应、模型失败、连接失败与契约错误，不需要在CI中调用真实DashScope。

### 实施过程中的修正

- Windows PowerShell 5.1的`Invoke-RestMethod`将UTF-8 JSON显示为乱码；使用项目实际采用的HTTPX读取同一SQLite历史后确认用户问题、回答和来源均为正常中文，因此没有修改正确的后端编码。
- FastAPI专项测试最初出现Starlette对旧`httpx`测试客户端的弃用警告；核对本机源码确认TestClient优先使用`httpx2`后，将其加入开发依赖并消除警告。
- Compose健康检查使用YAML的`>-`折叠标量传递单个Python命令字符串，实际配置解析、容器启动和健康检查均通过，无需因IDE提示改写业务逻辑。
- Ruff发现`app.py`导入块顺序不规范，使用自动修复后剩余错误为0。

### 验证结果

- 本机FastAPI和Streamlit双进程运行正常；真实聊天API生成正常中文答案，历史接口使用同一UUID恢复用户与助手消息。
- 停止FastAPI后，Streamlit显示安全的后端连接提示，不暴露HTTPX调用栈。
- HTTPX确认FastAPI响应编码为UTF-8，SQLite中的中文问题、回答和来源均未损坏。
- Swagger能够展示健康、聊天和会话历史接口。
- `docker compose config --quiet`无输出，服务列表为`api`和`web`。
- Compose双服务启动成功，`smart-cleaning-agent-api`和`smart-cleaning-agent-web`均为`healthy`。
- api/web容器内`pip check`均输出`No broken requirements found.`，两个进程均以`uid=999(appuser)`运行。
- web容器只获得`API_BASE_URL=http://api:8000`且没有模型密钥；api容器安全注入模型密钥但验证过程不打印真实值。
- api独占`storage`和`logs`挂载，web不挂载运行数据；最近100行容器日志没有`Traceback`、`PermissionError`或`ConnectionRefusedError`。
- Ruff全项目检查修复1个导入顺序问题后剩余错误为0。
- 完整自动化测试输出`76 passed`，不再出现Starlette TestClient弃用警告。
- Agent、FastAPI后端、HTTP前端客户端和核心模块共695条可执行语句、131条未覆盖，总覆盖率为81.15%，达到CI的60%门禁。
- 已暂存和未暂存补丁格式检查没有实际错误，仅有Dockerfile和`pyproject.toml`的本地LF/CRLF规范化提示。

### 相关提交

- 建议使用中文提交说明：`架构：使用FastAPI解耦前后端服务`

## 2026-08-08至2026-08-10：建立BM25与RRF混合检索实验基线

### 原问题

- 现有RAG只使用Chroma余弦向量检索，能够处理语义相近问法，但对明确术语、数字阈值和设备状态组合的召回不稳定。
- 60条端到端评估中的`low_battery_immediate_recharge`虽然命中了正确来源文件，但Top-10片段均未包含“电量低于20%优先回充”的直接证据，来源级Hit@K掩盖了片段漏召回。
- 余弦分数与BM25分数不在同一量纲，不能直接相加；项目也缺少可复现的候选数量和融合参数搜索过程。

### 修改内容

1. 固定`rank-bm25==0.2.2`，并通过安装预演、`pip check`和最小排序示例验证其与Python 3.13及现有NumPy版本兼容。
2. 新增确定性BM25分词器：使用NFKC统一全角字符和大小写，中文生成相邻双字词元，英文、百分比和带单位数字保持整体，避免依赖外部分词词典。
3. 新增内存BM25检索器，缓存全部知识片段词元，过滤没有任何词元交集的结果，并在同分时保持稳定文档顺序。
4. 为向量服务增加只读文档导出接口，从Chroma读取ID、正文和元数据但不加载1024维向量；校验三组数据数量一致，并保留ID用于跨检索渠道去重。
5. 新增RRF混合检索器，分别保留向量排名、BM25排名及两路原始分数，按Chroma文档ID合并候选，避免把不同量纲的原始分数直接相加。
6. 将候选召回与RRF融合拆开，A/B评测复用每个问题的一次向量结果，避免纯向量和混合检索各调用一次远程Embedding。
7. 新增纯向量与混合检索对比脚本，统一计算正例Hit@1、Hit@3和MRR，并列出排名提升、退化和不变用例；24条负例继续由端到端拒答评估负责，不使用RRF分数推导错误阈值。
8. 新增独立参数调优脚本，对向量候选`10/20`、BM25候选`5/10/20`和RRF常数`1/5/10/30/60`执行30组网格搜索；只缓存一次最大候选集合，后续组合均在本地计算。
9. 将最优参数保存到`config/chroma.yml`：向量候选10、BM25候选20、RRF常数10；评测脚本从统一配置读取，配置测试防止参数意外回退。
10. 人工复核高低温退化用例后，确认`维护保养.txt`和`扫地机器人100问2.txt`都完整回答问题，在评测集中补充第二个有效来源并增加标注回归测试。
11. 为中文分词、BM25检索、Chroma导出、RRF融合、指标计算、参数排序和评测标注增加不访问真实模型的单元测试。

### 修改意义

- BM25补充了向量检索对明确词语、百分比和数字条件不敏感的问题，RRF则提供不依赖原始分数量纲的稳定融合方式。
- 参数选择来自同一套36条正例和30组组合实验，而不是凭经验填写；独立调优脚本使参数范围、排序规则和推荐结果可以复现。
- 修正后的来源级指标从纯向量Hit@1 80.56%、Hit@3 94.44%、MRR 0.8750，提升到混合检索Hit@1 88.89%、Hit@3 97.22%、MRR 0.9306。
- 片段级诊断显示低电量20%直接证据已经从向量Top-10之外提升到BM25第1名，证明混合召回解决了候选缺失；但该片段在RRF最终排名为第6，下一阶段需要重排序而不是继续放宽拒答规则。
- HEPA维护周期的完整证据进入融合第4名，成为另一个固定重排序样本；高低温用例则通过人工内容核验识别为标注不完整，而不是算法真实退化。

### 当前边界

- 当前混合检索已形成离线候选召回和A/B实验模块，尚未替换生产RAG的Top-3向量检索路径。
- 来源级Hit@K仍不能单独证明片段包含完整答案；低电量和HEPA用例继续用于片段级重排序验收。
- RRF只依据两路排名，不理解片段是否完整覆盖问题中的多个条件，后续需要增加重排序并重新运行端到端可回答性评估。

### 验证结果

- 真实Chroma只读导出326个片段，文档ID全部存在且唯一，空正文和缺少来源数量均为0。
- 修正标注后的30组参数搜索推荐向量候选10、BM25候选20、RRF常数10。
- 最优参数在36条正例上取得Hit@1 88.89%、Hit@3 97.22%、MRR 0.9306，相比纯向量基线分别提高8.33个百分点、2.78个百分点和0.0556。
- 最优参数产生5条来源排名提升、1条真实退化和30条不变；真实退化为完整HEPA维护证据从向量第1降至融合第4。
- 低电量20%直接证据位于BM25第1、融合第6，确认下一阶段优化目标位于候选重排序。
- Ruff全项目检查输出`All checks passed!`；完整自动化测试输出`102 passed`。
- Agent、FastAPI后端、HTTP前端客户端和核心模块共809条可执行语句、133条未覆盖，总覆盖率为83.56%，达到CI的60%门禁。
- Git已暂存和未暂存差异检查没有实际错误，仅有两个本地CRLF与仓库LF规范化提示。

### 相关提交

- 建议使用中文提交说明：`检索：新增BM25与RRF混合召回实验`

## 2026-08-10至2026-08-12：接入qwen3-rerank并完成生产检索链路

### 原问题

- BM25与RRF已经将低电量20%直接证据召回到候选池，但该片段位于融合第6，生产RAG原有Top-3向量路径仍无法回答固定困难用例。
- 完整HEPA维护周期证据位于融合第4，说明RRF只能综合两路排名，不能判断片段是否完整覆盖问题中的多个条件。
- 重排序依赖外部HTTP服务，需要明确的超时、错误边界、资源复用和降级策略，不能让排序服务暂时不可用导致整个知识问答失败。
- 原RAG服务在模块导入时立即初始化，接入重排序后会使普通工具测试和CI导入阶段依赖本地密钥及业务空间地址。

### 修改内容

1. 在`config/rag.yml`增加qwen3-rerank模型名、候选数量10、最终Top-3、30秒超时和面向直接问答证据的排序指令。
2. 在`.env.example`增加`DASHSCOPE_RERANK_BASE_URL`占位符；重排序使用百炼业务空间专用`compatible-api/v1`地址，不把真实工作空间ID写入仓库。
3. 新增`rag/reranker.py`，使用复用的HTTPX连接池调用qwen3-rerank，校验响应下标与分数，并把远程错误转换为不暴露密钥和请求正文的领域异常。
4. 支持通过环境字典和MockTransport注入重排序客户端，使测试不访问真实网络，并提供上下文管理器和幂等关闭能力。
5. 新增`evaluation/compare_reranking.py`，在每条正例只调用一次Embedding的前提下，对比纯向量、BM25与RRF混合召回、候选Recall@10和重排序Top-3。
6. 新增`rag/retrieval_pipeline.py`，正式串联向量Top-10、BM25 Top-20、RRF Top-10和qwen3-rerank Top-3；向量侧保留低分粗过滤，BM25仍可补回数字和关键词证据。
7. 重排序请求失败时降级为RRF Top-3并记录不包含用户问题的警告，避免外部排序服务成为知识问答单点故障。
8. 将`RagSummarizeService`从直接依赖Chroma改为依赖完整检索流水线，服务层继续负责资料充分性判断、统一拒答和可追溯来源。
9. 将Agent工具中的RAG服务改为带锁的延迟初始化单例；模块导入时不连接Chroma或重排序接口，首次知识工具调用时才创建，后续请求复用同一实例。
10. 扩展`ReactAgent.close()`，由现有FastAPI生命周期同时释放SQLite连接和共享RAG重排序HTTP连接池。
11. 增加重排序客户端、检索流水线、降级路径、服务接入、延迟初始化、资源复用和幂等关闭测试。
12. 更新README中的生产检索流程、环境配置、A/B指标、端到端结果、资源生命周期和后续查询改写计划。

### 修改意义

- 重排序从10条高召回候选中选择最能直接回答问题的3条，使生成模型获得更完整的证据，而不是继续扩大上下文或放宽拒答规则。
- 同一套36条正例形成“纯向量、混合检索、重排序”的可复现实验链，能够在简历和面试中用指标说明每个算法模块的作用。
- 低电量20%和HEPA周期两个固定困难样本都从RRF Top-3之外提升到重排序第1，证明提升来自证据排序而不是修改问题或答案。
- 延迟初始化让普通Agent工具和CI测试不依赖本地重排序地址；连接复用和应用退出释放避免每次请求重复建连或遗留资源。
- 降级机制保持服务可用性，同时保留System级资料充分性判断，因此重排序失败不会自动放宽知识边界。

### 验证结果

- qwen3-rerank真实接口返回HTTP 200，最佳相关片段分数为0.9709316。
- 36条正例的混合候选Recall@10为100%，说明重排序输入候选没有遗漏已标注来源。
- 纯向量基线为Hit@1 80.56%、Hit@3 94.44%、MRR 0.8750。
- BM25与RRF混合Top-3为Hit@1 88.89%、Hit@3 97.22%、MRR 0.9306。
- qwen3-rerank Top-3为Hit@1 88.89%、Hit@3 100%、MRR 0.9398；相对混合检索的Hit@3提高2.78个百分点，MRR提高0.0093。
- 低电量20%直接证据由RRF第6提升到重排序第1，完整HEPA维护周期证据由RRF第4提升到重排序第1。
- 正式LangChain工具入口验证使用`HybridRerankRetriever`和`QwenReranker`，正确回答低于20%优先回充并附加来源；关闭后共享服务引用被清空。
- 真实模型冒烟评估为3/3通过；60条全量端到端评估为60/60通过，进程退出码为0。
- 重排序器与检索流水线专项测试为15项通过；接入RAG服务后的相关测试为23项通过；延迟初始化和资源生命周期相关测试为33项通过。
- `pip check`输出`No broken requirements found.`，运行依赖不存在缺失或版本冲突。
- Ruff全项目检查输出`All checks passed!`。
- 完整自动化测试输出`120 passed`，核心模块总覆盖率为83.67%，达到持续集成要求的60%门禁。
- 已暂存和未暂存补丁格式检查没有实际错误，仅有两个本地CRLF与仓库LF规范化提示。

### 相关提交

- 建议使用中文提交说明：`检索：接入重排序并启用生产混合链路`
