# Web UI 与 Agent 编排。
streamlit==1.60.0
langchain==1.3.14
langgraph==1.2.9

# 使用本地SQLite持久化LangGraph会话，应用重启后仍可读取检查点。
langgraph-checkpoint-sqlite==3.1.0

# 模型集成、向量库和文本处理。
# 通过DashScope官方OpenAI兼容接口调用聊天模型和向量模型。
langchain-openai==1.4.1
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2
chromadb==1.5.9

# 环境变量、YAML 与 PDF 读取。
python-dotenv==1.2.2
PyYAML==6.0.3
pypdf==6.14.2
